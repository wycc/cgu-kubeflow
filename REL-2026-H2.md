# Release ietms for the 2026 H2 release
* Support remote SSH access. We can enable the ssh port in the jupyter server UI.(品沅/柏文)
  * We can use the remote SSH inside the VS Code to write program and use AI agent to edit files inside the notebook.
* User CAN CHANGE THE cpu/gpu/MEMORY setting after it was created.(品沅)
* Support public service to the external internet. This can be configured in the account manager.(柏文)
  * We can use the notebook to host a service and ask the system adnimistrator to public it to the external world.
* Use the open code to manager the services in a chatbot via MCP and skills.(韋傑)
  * SA can use open code to check the status of the cluster and fix issues inside it. We have write multiple skills to handle the frequent issues.
* Make announcements in the account manager(Lance)
  * display important notification in the home page of the kubeflow.
* Support shared folder to shared data within the entire cluster or group.(竑安)
  * TA can put files inside the shared folder so that every students can use it.
* Improve cost report. We can access the cost inside the account manager without using the grafana.(竑安)
  * Generate usage report monthly.
 
